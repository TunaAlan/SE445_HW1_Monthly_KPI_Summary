schedule==1.2.2

# Step 3 — Google Sheets connector (real mode only; not needed for --mock)
gspread==6.1.2
google-auth==2.29.0

# Step 4 — Gemini AI completion
google-genai>=0.8.0
python-dotenv==1.0.1
